import { useId, type SelectHTMLAttributes } from "react";
interface Option{value:string;label:string}
interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement>{label?:string;options:readonly Option[];error?:string;helperText?:string}
export default function Select({label,options,error,helperText,className="",id,...props}:SelectProps){const generated=useId(),selectId=id??generated,errorId=`${selectId}-error`,helperId=`${selectId}-helper`;return <div className="space-y-1.5">{label&&<label htmlFor={selectId} className="block text-sm font-medium text-slate-700">{label}{props.required&&<span aria-hidden="true" className="text-red-600"> *</span>}</label>}<select id={selectId} aria-invalid={!!error} aria-describedby={error?errorId:helperText?helperId:props["aria-describedby"]} className={`min-h-11 w-full rounded-lg border bg-white px-3 py-2 text-slate-900 outline-none transition ${error?"border-red-500 focus:ring-red-200":"border-slate-300 focus:border-blue-500 focus:ring-blue-200"} focus:ring-2 disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-500 ${className}`} {...props}>{options.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select>{helperText&&!error&&<p id={helperId} className="text-sm text-slate-500">{helperText}</p>}{error&&<p id={errorId} role="alert" className="text-sm text-red-700">{error}</p>}</div>}
