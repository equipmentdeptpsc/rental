import { useEffect, useRef, useState } from "react";
import { useNavigate } from "react-router-dom";
import { useApplicationDependenciesCompatibility } from "@/app/composition";
import { useAuth } from "@/features/auth/AuthContext";
import { useFormSubmission } from "@/components/form/useFormSubmission";
import Button from "@/components/ui/Button";
import Input from "@/components/ui/Input";
import { EQUIPMENT_MAINTENANCE_TYPES, type EquipmentReferenceData } from "@/features/equipment/commands/contracts";
import { requestCanonicalEquipmentRefresh } from "@/features/equipment/remote/canonicalEquipmentRefresh";
import type { CanonicalEquipmentCategory, CanonicalEquipmentSubcategory } from "@/features/masters/equipment-subcategory/canonical";
import { EquipmentSubcategoryDrawer } from "@/features/masters/equipment-subcategory/EquipmentSubcategoryDrawer";

export default function RemoteEquipmentForm() {
  const deps = useApplicationDependenciesCompatibility(); const repository = deps.commandRepositories.canonicalEquipment!; const { hasPermission } = useAuth(); const navigate = useNavigate();
  const [reference, setReference] = useState<{ loading: boolean; data?: EquipmentReferenceData; error?: string }>({ loading: true }); const [categories, setCategories] = useState<CanonicalEquipmentCategory[]>([]); const [subcategory, setSubcategory] = useState<{ loading: boolean; items: CanonicalEquipmentSubcategory[]; error?: string }>({ loading: false, items: [] }); const [subcategoryDrawerOpen, setSubcategoryDrawerOpen] = useState(false); const [subcategoryRefresh, setSubcategoryRefresh] = useState(0);
  const [form, setForm] = useState({ assetNo: "", equipmentName: "", categoryId: "", subcategoryId: "", maintenanceType: "Engine Hours" as typeof EQUIPMENT_MAINTENANCE_TYPES[number], costCodeId: "", currentReading: "", remarks: "" }); const identity = useRef<{ equipmentId: string; commandId: string; idempotencyKey: string } | undefined>(undefined);
  useEffect(() => { let active = true; void Promise.all([repository.readReferenceData(), deps.readRepositories.equipmentCategories.list()]).then(([referenceResult, categoriesResult]) => { if (!active) return; setReference(referenceResult.success ? { loading: false, data: { costCodes: referenceResult.costCodes } } : { loading: false, error: referenceResult.message }); if (categoriesResult.success) setCategories(categoriesResult.value.items); else setReference({ loading: false, error: "Equipment Categories could not be loaded." }); }); return () => { active = false; }; }, [deps.readRepositories.equipmentCategories, repository]);
  useEffect(() => { let active = true; if (!form.categoryId) { setSubcategory({ loading: false, items: [] }); return; } setSubcategory({ loading: true, items: [] }); void deps.readRepositories.equipmentSubcategories.listAssignable(form.categoryId).then((result) => { if (active) setSubcategory(result.success ? { loading: false, items: result.value } : { loading: false, items: [], error: result.error.message }); }); return () => { active = false; }; }, [deps.readRepositories.equipmentSubcategories, form.categoryId, subcategoryRefresh]);
  const submission = useFormSubmission("Equipment", async () => { const command = identity.current ??= { equipmentId: crypto.randomUUID(), commandId: crypto.randomUUID(), idempotencyKey: crypto.randomUUID() }; const result = await repository.createEquipment({ ...command, assetNo: form.assetNo.trim(), equipmentName: form.equipmentName.trim(), categoryId: form.categoryId, subcategoryId: form.subcategoryId, maintenanceType: form.maintenanceType, costCodeId: form.costCodeId, ...(form.currentReading.trim() ? { currentReading: Number(form.currentReading) } : {}), ...(form.remarks.trim() ? { remarks: form.remarks.trim() } : {}) }); if (!result.success) throw new Error(result.message); requestCanonicalEquipmentRefresh(); navigate("/equipment"); });
  if (reference.loading) return <div className="p-8 text-slate-500">Loading Equipment reference data…</div>;
  if (!reference.data) return <div className="p-8" role="alert">{reference.error ?? "Equipment reference data could not be loaded."}</div>;
  if (!reference.data.costCodes.length) return <div className="mx-auto max-w-3xl space-y-3 p-8" role="status"><h1 className="text-3xl font-bold">New Equipment</h1><p>No active Cost Codes are available. A canonical Cost Code must be configured before Equipment can be created.</p></div>;
  const noCategories = !categories.length;
  return <div className="mx-auto max-w-3xl space-y-6 p-8"><div><h1 className="text-3xl font-bold">New Equipment</h1><p className="mt-2 text-gray-500">Create canonical remote Equipment.</p></div>{noCategories && <p className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800" role="status">Create or activate an Equipment Category before creating Equipment.</p>}<form className="space-y-5" onSubmit={(event) => { event.preventDefault(); if (!form.assetNo.trim() || !form.equipmentName.trim() || !form.costCodeId || !form.categoryId || !form.subcategoryId) return submission.fail(!form.categoryId ? "Select an Equipment Category." : !form.subcategoryId ? "Select an Equipment Sub-Category." : "Enter Asset Number, Equipment Name, and Cost Code."); if (form.currentReading.trim() && (!Number.isFinite(Number(form.currentReading)) || Number(form.currentReading) < 0)) return submission.fail("Current Reading must be zero or greater."); void submission.submit(undefined); }}>{submission.feedback}<Input label="Asset Number" required value={form.assetNo} onChange={(event) => setForm({ ...form, assetNo: event.target.value })} /><Input label="Equipment Name" required value={form.equipmentName} onChange={(event) => setForm({ ...form, equipmentName: event.target.value })} /><label className="block text-sm font-medium">Equipment Category<select required aria-label="Equipment Category" className="app-control mt-1 w-full" value={form.categoryId} onChange={(event) => setForm({ ...form, categoryId: event.target.value, subcategoryId: "" })}><option value="">Select Equipment Category</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><div><label className="block text-sm font-medium">Equipment Sub-Category<select required aria-label="Equipment Sub-Category" disabled={!form.categoryId || subcategory.loading} className="app-control mt-1 w-full" value={form.subcategoryId} onChange={(event) => setForm({ ...form, subcategoryId: event.target.value })}><option value="">{subcategory.loading ? "Loading Sub-Categories…" : form.categoryId ? "Select Equipment Sub-Category" : "Select a Category first"}</option>{subcategory.items.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label>{subcategory.error && <p className="mt-1 text-sm text-red-700" role="alert">Equipment Sub-Categories could not be loaded.</p>}{hasPermission("masterData.create") && <button type="button" className="mt-2 text-sm text-blue-700 underline" onClick={() => setSubcategoryDrawerOpen(true)}>+ Add new sub-category</button>}</div><label className="block text-sm font-medium">Maintenance Type<select className="app-control mt-1 w-full" value={form.maintenanceType} onChange={(event) => setForm({ ...form, maintenanceType: event.target.value as typeof form.maintenanceType })}>{EQUIPMENT_MAINTENANCE_TYPES.map((value) => <option key={value}>{value}</option>)}</select></label><label className="block text-sm font-medium">Cost Code<select required className="app-control mt-1 w-full" value={form.costCodeId} onChange={(event) => setForm({ ...form, costCodeId: event.target.value })}><option value="">Select Cost Code</option>{reference.data.costCodes.map((item) => <option key={item.id} value={item.id}>{item.code} — {item.name}</option>)}</select></label><Input label="Current Reading" type="number" min="0" value={form.currentReading} onChange={(event) => setForm({ ...form, currentReading: event.target.value })} /><Input label="Remarks" value={form.remarks} onChange={(event) => setForm({ ...form, remarks: event.target.value })} /><div className="flex justify-end"><Button type="submit" disabled={submission.busy || noCategories}>{submission.busy ? "Saving..." : "Create Equipment"}</Button></div></form><EquipmentSubcategoryDrawer open={subcategoryDrawerOpen} onClose={() => setSubcategoryDrawerOpen(false)} categories={categories} initialCategoryId={form.categoryId} onSaved={() => setSubcategoryRefresh((value) => value + 1)} /></div>;
}
