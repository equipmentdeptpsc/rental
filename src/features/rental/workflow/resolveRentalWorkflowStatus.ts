import type{RentalRecord}from"../types";import type{DeurRecord}from"../deur/types";
export type RentalWorkflowStage="Draft"|"Reserved"|"AwaitingManagerApproval"|"ManagerRejected"|"ApprovedForRelease"|"Released"|"Active"|"DeurInProgress"|"AwaitingCustomerAcknowledgement"|"CustomerCorrectionRequested"|"BillingEligible"|"Billed"|"Returned"|"Closed";
export function resolveRentalWorkflowStatus(input:{rental:RentalRecord;effectiveDeur?:DeurRecord;commercialTermsAvailable:boolean;billableEvidence:boolean}){const{rental,effectiveDeur:deur}=input;let stage:RentalWorkflowStage,label="",explanation="",nextAction="",blockingReasons:string[]=[];if(rental.status==="Closed"){stage="Closed";label="Closed";explanation="Rental workflow is complete.";nextAction="None"}else if(rental.status==="Returned"){stage="Returned";label="Returned";explanation="Equipment has been returned.";nextAction="Close Rental"}else if(deur?.status==="Billed"||deur?.billingLocked){stage="Billed";label="Billed";explanation="The effective DEUR has been consumed by Billing.";nextAction="Review Billing Statement"}else if(deur?.status==="Rejected"){stage="CustomerCorrectionRequested";label="Customer Correction Requested";explanation=deur.rejectionReason??"The Customer requested a correction.";nextAction="Correct DEUR and resubmit"}else if(deur?.status==="Submitted"){stage="AwaitingCustomerAcknowledgement";label="Awaiting Customer Acknowledgement";explanation="The submitted DEUR requires a Customer decision.";nextAction="Send or open Customer review request"}else if(deur?.status==="Acknowledged"&&input.commercialTermsAvailable&&input.billableEvidence){stage="BillingEligible";label="Billing Eligible";explanation="The effective acknowledged DEUR has Commercial Terms and billable evidence.";nextAction="Generate Billing"}else if(deur&&["Draft","In Progress"].includes(deur.status)){stage="DeurInProgress";label="DEUR In Progress";explanation="Daily operational evidence is being recorded.";nextAction="Complete and submit DEUR"}else if(rental.status==="Active"){stage="Active";label="Active";explanation="Rental operations are active.";nextAction="Record DEUR"}else if(rental.status==="Released"){stage="Released";label="Released";explanation="Equipment has been released.";nextAction="Activate Rental"}else if(rental.approvalStatus==="Pending"){stage="AwaitingManagerApproval";label="Awaiting Manager Approval";explanation="Manager approval is pending.";nextAction="Manager approves or rejects"}else if(rental.approvalStatus==="Rejected"){stage="ManagerRejected";label="Manager Rejected";explanation=rental.approvalDecisionRemarks??"Manager rejected the Rental.";nextAction="Revise and resubmit"}else if(rental.approvalStatus==="Approved"){stage="ApprovedForRelease";label="Approved for Release";explanation="Manager approval authorizes release eligibility.";nextAction="Release Equipment"}else if(rental.status==="Reserved"){stage="Reserved";label="Reserved";explanation="Rental is reserved.";nextAction="Send to Approver"}else{stage="Draft";label="Draft";explanation="Rental preparation is incomplete.";nextAction="Complete and reserve Rental"}if(deur?.status==="Acknowledged"&&!input.commercialTermsAvailable)blockingReasons.push("Commercial Terms are unavailable for the effective Rental Equipment Line.");if(deur?.status==="Acknowledged"&&!input.billableEvidence)blockingReasons.push("The acknowledged DEUR has no calculable billable evidence.");return{stage,label,explanation,recommendedNextAction:nextAction,blockingReasons}}
