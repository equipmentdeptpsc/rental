import type{RentalRecord}from"../types";
export interface RentalCustomerContactInput{representativeName:string;representativeEmail:string;designation?:string;contactNumber?:string}
export function updateRentalCustomerContact(rental:RentalRecord,input:RentalCustomerContactInput,actor:{id?:string;name:string;role?:string},timestamp:string,pendingRequest=false){if(actor.role!=="Admin")return{success:false as const,message:"Only an Admin can edit the Rental Customer Contact."};if(pendingRequest)return{success:false as const,message:"Resolve or replace the current Pending Customer review request before changing its recipient."};const name=input.representativeName.trim(),email=input.representativeEmail.trim().toLocaleLowerCase();if(!name)return{success:false as const,message:"Representative Name is required."};if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return{success:false as const,message:"A valid Representative Email is required."};return{success:true as const,rental:{...rental,customerContactSnapshot:{representativeName:name,representativeEmail:email,designation:input.designation?.trim()||undefined,contactNumber:input.contactNumber?.trim()||undefined,capturedAt:rental.customerContactSnapshot?.capturedAt??timestamp,updatedAt:timestamp,updatedBy:actor.name}}}}
export const maskEmail=(email:string|undefined)=>{if(!email)return"not set";const[a,b]=email.split("@");return`${a.slice(0,1)}***@${b??"***"}`};
