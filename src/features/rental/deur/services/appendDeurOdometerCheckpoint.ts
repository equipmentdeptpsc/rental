import type { DeurOdometerCheckpoint,DeurRecord } from "../types";
import { buildDeurOdometerTripEvidence } from "./buildDeurOdometerTripEvidence";
import { normalizeOdometerTripEvidence } from "./deurEvidence";
export function appendDeurOdometerCheckpoint(record:DeurRecord,checkpoint:DeurOdometerCheckpoint){if(record.evidenceMode!=="ODOMETER_TRIP")return{success:false as const,message:"DEUR is not using Odometer/Trip evidence."};if(!["Draft","In Progress"].includes(record.status))return{success:false as const,message:"DEUR evidence is not editable."};const checkpoints=[...(record.odometerTripEvidence?.checkpoints??[]),structuredClone(checkpoint)];const normalized=checkpoints.length<2?normalizeOdometerTripEvidence({checkpoints}):undefined;const built=checkpoints.length>=2?buildDeurOdometerTripEvidence(checkpoints):undefined;if(built&&!built.success)return{success:false as const,message:built.issues[0]};const evidence=built?.success?built.evidence:normalized;if(!evidence)return{success:false as const,message:"Checkpoint is invalid."};return{success:true as const,record:{...structuredClone(record),odometerTripEvidence:evidence,status:"In Progress" as const,updatedAt:new Date().toISOString()}}}
