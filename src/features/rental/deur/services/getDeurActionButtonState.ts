import type { DeurActivityTypeCanonical, DeurRecord } from "../types"; import { deriveDeurEventState } from "./deriveDeurEventState";
export function getDeurActionButtonState(record: DeurRecord, activityType: DeurActivityTypeCanonical) { const state=deriveDeurEventState(record); if(record.legacy || ["Submitted","Acknowledged","Rejected"].includes(record.status)) return {label:"Unavailable",action:"start" as const,enabled:false,disabledReason:"DEUR is not editable.",automaticTransition:false}; if(activityType==="shift") return state.shiftCompleted?{label:"Shift Completed",action:"end" as const,enabled:false,disabledReason:"Shift has ended.",automaticTransition:false}:state.shiftOpen?{label:"End Shift",action:"end" as const,enabled:true,automaticTransition:Boolean(state.openPrimaryActivity)}:{label:"Start Shift",action:"start" as const,enabled:true,automaticTransition:false}; if(!state.shiftOpen)return {label:`Start ${activityType}`,action:"start" as const,enabled:false,disabledReason:"Start Shift first.",automaticTransition:false}; return state.openPrimaryActivity===activityType?{label:`End ${activityType}`,action:"end" as const,enabled:true,automaticTransition:false}:{label:`Start ${activityType}`,action:"start" as const,enabled:true,automaticTransition:Boolean(state.openPrimaryActivity)}; }
