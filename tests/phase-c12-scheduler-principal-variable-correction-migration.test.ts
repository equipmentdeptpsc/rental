import{readFileSync}from"node:fs";import{expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260803006800_phase_c12_grouped_review_scheduler_principal_variable_correction.sql","utf8");it("removes the principal id ambiguity without changing authority",()=>{expect(sql).toContain("target_principal_id");expect(sql).toContain("TO service_role");expect(sql).not.toMatch(/TO authenticated|DISABLE TRIGGER/i);});
