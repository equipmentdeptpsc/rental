import { describe, expect, it } from "vitest";
import { applyDeurAction } from "@/features/rental/deur/services/deurEventStateMachine";
const r:any={id:"d",rentalId:"r",equipmentId:"e",operatorId:"o",workDate:"2026-01-01",logs:[],totalOperatingMinutes:0,totalIdleMinutes:0,totalMaintenanceMinutes:0,totalMealBreakMinutes:0,totalMobilizationMinutes:0,totalDemobilizationMinutes:0,status:"Draft",createdAt:"",updatedAt:"",events:[]};
const act=(record:any,activityType:any,action:any,timestamp="2026-01-01T00:00:00.000Z")=>applyDeurAction(record,{activityType,action,timestamp,idFactory:()=>crypto.randomUUID()});
describe("DEUR event state machine",()=>{it("starts shift and rejects duplicates or activity before shift",()=>{expect(act(r,"operation","start").success).toBe(false);const s=act(r,"shift","start");expect(s.success).toBe(true);if(s.success)expect(act(s.record,"shift","start").success).toBe(false)});it("transitions operation to idle and auto-closes on shift end",()=>{const s=act(r,"shift","start");if(!s.success)return;const o=act(s.record,"operation","start");if(!o.success)return;const i=act(o.record,"idle","start","2026-01-01T01:00:00.000Z");expect(i.success).toBe(true);if(!i.success)return;expect(i.createdEvents).toHaveLength(2);const end=act(i.record,"shift","end","2026-01-01T02:00:00.000Z");expect(end.success).toBe(true);if(end.success)expect(act(end.record,"operation","start").success).toBe(false)});});
