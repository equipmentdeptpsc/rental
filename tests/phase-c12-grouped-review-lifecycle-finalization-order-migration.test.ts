import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260803006600_phase_c12_grouped_review_batch_lifecycle_finalization_order.sql","utf8");
describe("C12 grouped lifecycle finalization order correction",()=>{it("clones immutable items before finalizing the successor",()=>{expect(sql).toContain("FROM erp.customer_review_batch_items WHERE batch_id=source.id;");expect(sql).toContain("SET finalized_at=now_at WHERE id=successor.id");expect(sql.indexOf("FROM erp.customer_review_batch_items")).toBeLessThan(sql.indexOf("SET finalized_at=now_at"));});it("preserves the authenticated-only command grant",()=>{expect(sql).toContain("FROM PUBLIC,anon,service_role");expect(sql).toContain("TO authenticated");expect(sql).not.toMatch(/DISABLE TRIGGER|session_replication_role/i);});});
