import { readFileSync } from "node:fs"; import { describe, expect, it } from "vitest";
const source=readFileSync("worker/uatScenarioDeurInspection.ts","utf8"), index=readFileSync("worker/index.ts","utf8"), migration=readFileSync("supabase/migrations/20260830001600_isolated_uat_scenario_deur_read.sql","utf8");
describe("isolated UAT scenario DEUR read boundary",()=>{it("is fixed-scope, authenticated, read-only, and line/date aware",()=>{expect(source).toContain("DEUR-2026-000003");expect(source).toContain("inspect_isolated_uat_scenario_deur");expect(index).toContain("/api/admin/uat/inspect-scenario-deur");expect(migration).toContain("rental_equipment_line_id=ANY(target_lines)");expect(migration).toContain("activityTimelineCount");expect(migration).toContain("REVOKE ALL ON FUNCTION");expect(migration).toContain("GRANT EXECUTE ON FUNCTION erp.inspect_isolated_uat_scenario_deur(jsonb) TO service_role");});it("keeps lifecycle rows separate from canonical operational activity readback",()=>{const extended=readFileSync("supabase/migrations/20260830001700_extend_uat_scenario_deur_activity_read.sql","utf8");expect(extended).toContain("lifecycleEventCount");expect(extended).toContain("activityRows");expect(extended).toContain("isOperational");expect(extended).toContain("ev.activity_type IN('operation','idle','standby','mealBreak','breakdown')");expect(extended).toContain("REVOKE ALL ON FUNCTION");});});
