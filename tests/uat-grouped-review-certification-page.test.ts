import{readFileSync}from"node:fs";import{expect,it}from"vitest";
it("uses the canonical browser session and keeps delivery disabled until MATCH",()=>{const page=readFileSync("src/pages/UatGroupedReviewCertification.tsx","utf8"),repo=readFileSync("src/integrations/supabase/SupabaseUatGroupedReviewCertification.ts","utf8"),router=readFileSync("src/app/router.tsx","utf8");expect(repo).toContain("client.auth.getSession()");expect(repo).toContain("authorization:`Bearer ${token}`");expect(page).toContain('disabled={state!=="MATCH"}');expect(page).toContain("Generate and deliver exactly one review");expect(router).toContain('permitted("settings.update"');expect(page+repo).not.toMatch(/localStorage|service.role|console\./i);});
