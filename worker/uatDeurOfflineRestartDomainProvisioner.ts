import { createClient } from '@supabase/supabase-js';
import type { GroupedReviewWorkerEnvironment } from './configuration';
type Json=Record<string,unknown>;const KEY='DEUR-OFFLINE-RESTART-RUNTIME-CERT-2026-09-01',PROFILE='UAT_DEUR_OFFLINE_RESTART_RUNTIME_V1',DATE='2026-09-01';const out=(status:number,body:Json)=>({status,body});const fixed=(body:unknown)=>Boolean(body&&typeof body==='object'&&!Array.isArray(body)&&Object.keys(body as Json).length===2&&(body as Json).scenarioKey===KEY&&(body as Json).profileVersion===PROFILE);
async function refs(service:any,tenant:string){const r=await service.schema('erp').rpc('resolve_uat_deur_offline_restart_runtime_references',{command:{companyId:tenant,scenarioKey:KEY,profileVersion:PROFILE}});const d=r.data as Json|null;return{error:Boolean(r.error||!d),data:d,ready:d?.referencesReady===true};}
async function canonical(client:any,stage:string,name:string,command:Json){const r=await client.schema('erp').rpc(name,{command});if(r.error||(r.data as Json)?.success!==true)throw new Error(`${stage}:${String((r.data as Json)?.code??'CANONICAL_COMMAND_FAILED')}`);}
function safeClaimFailure(claim:{data:unknown;error:unknown}){const data=claim.data as Json|null;const error=claim.error as {code?:unknown}|null;const claimCode=typeof data?.code==='string'?data.code:typeof error?.code==='string'?error.code:'SCENARIO_CLAIM_FAILED';return{success:false,code:'UAT_OFFLINE_RESTART_SCENARIO_FAILED',failedStage:'CLAIM',failedCode:claimCode,claimState:typeof data?.state==='string'?data.state:null,scenarioExists:data?.scenario!==undefined,profileMatches:claimCode!=='SCENARIO_PROFILE_MISMATCH',resumeEligible:data?.state==='PROVISIONING'};}
async function actor(request:Request,env:GroupedReviewWorkerEnvironment){if(!env.ENABLE_UAT_SYNTHETIC_PROVISIONER||!env.SUPABASE_URL||!env.SUPABASE_SERVICE_ROLE_KEY||!env.SUPABASE_PUBLISHABLE_KEY)return null;const token=request.headers.get('authorization')?.match(/^Bearer (.+)$/i)?.[1];if(!token)return null;const service=createClient(env.SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const identity=await service.auth.getUser(token);if(identity.error||!identity.data.user)return null;const user=await service.schema('erp').from('users').select('company_id,status').eq('id',identity.data.user.id).eq('status','active').maybeSingle();const permission=await service.schema('erp').from('effective_user_permissions').select('permission_code').eq('user_id',identity.data.user.id).eq('permission_code','settings.update').maybeSingle();if(user.error||!user.data||permission.error||!permission.data)return null;return{service,token,tenant:String(user.data.company_id)};}
async function inspect(service:any,tenant:string){const r=await service.schema('erp').rpc('inspect_uat_deur_offline_restart_runtime_scenario',{command:{companyId:tenant,scenarioKey:KEY,profileVersion:PROFILE}});return r.error?out(503,{success:false,code:'READ_FAILED'}):out(200,r.data as Json);}
export async function provisionUatDeurOfflineRestartDomain(request:Request,env:GroupedReviewWorkerEnvironment){if(!fixed(await request.json().catch(()=>null)))return out(400,{success:false,code:'VALIDATION_REJECTED'});const a=await actor(request,env);if(!a)return out(401,{success:false,code:'UNAUTHENTICATED'});const reference=await refs(a.service,a.tenant);if(reference.error||!reference.ready)return out(409,{success:false,code:'UAT_REFERENCE_UNAVAILABLE'});const rr=reference.data!;const claim=await a.service.schema('erp').rpc('claim_uat_deur_offline_restart_runtime_scenario',{command:{companyId:a.tenant,scenarioKey:KEY,profileVersion:PROFILE,references:{costCodeId:rr.costCodeId,activityCodeId:rr.activityCodeId,workDescriptionId:rr.workDescriptionId}}});if(claim.error||(claim.data as Json)?.success!==true)return out(409,safeClaimFailure(claim));const s=(claim.data as Json).scenario as Json;if((claim.data as Json).state==='DOMAIN_READY')return inspect(a.service,a.tenant);const client=createClient(env.SUPABASE_URL!,env.SUPABASE_PUBLISHABLE_KEY!,{global:{headers:{Authorization:`Bearer ${a.token}`}},auth:{persistSession:false,autoRefreshToken:false}});const id=(stage:string,entity:string)=>({commandId:`UAT-OFFLINE-RESTART-${stage}-${s[entity+'Id']}`,idempotencyKey:`uat-offline-restart:${stage.toLowerCase()}:${s[entity+'Id']}`});try{await canonical(client,'CUSTOMER','command_create_customer',{...id('CUSTOMER','customer'),customerId:s.customerId,customerCode:'UAT-OFFLINE-RESTART-CUSTOMER-001',name:'Synthetic UAT Offline Restart Customer 001'});await canonical(client,'PROJECT','command_create_project',{...id('PROJECT','project'),projectId:s.projectId,projectCode:'UAT-OFFLINE-RESTART-SITE-001',name:'Synthetic UAT Offline Restart Certification Site 001',customerId:s.customerId,location:'Isolated UAT'});await canonical(client,'EQUIPMENT','command_create_equipment',{...id('EQUIPMENT','equipment'),equipmentId:s.equipmentId,assetNo:'UAT-OFFLINE-RESTART-EQ-001',equipmentName:'Synthetic UAT Offline Restart Equipment 001',maintenanceType:'Engine Hours',costCodeId:rr.costCodeId,currentReading:0,remarks:'Synthetic isolated-UAT offline restart certification equipment.'});await canonical(client,'ASSIGNMENT','command_create_assignment',{...id('ASSIGNMENT','assignment'),assignmentId:s.assignmentId,equipmentId:s.equipmentId,operatorId:s.operatorId,projectId:s.projectId,activityCodeId:rr.activityCodeId,assignedDate:DATE,expectedReturn:DATE,remarks:'Synthetic offline restart certification assignment.'});await canonical(client,'RENTAL','command_create_reserved_rental',{...id('RENTAL','rental'),rentalId:s.rentalId,rentalNumber:'UAT-OFFLINE-RESTART-20260901',customerId:s.customerId,projectId:s.projectId,dateOut:DATE,expectedReturn:DATE,rentalType:'Operated Rental',lines:[{id:s.lineId,equipmentId:s.equipmentId,assignmentId:s.assignmentId,operatorId:s.operatorId}]});await canonical(client,'PREPARE','command_prepare_reserved_rental_aggregate',{...id('PREPARE','rental'),expectedRentalVersion:1,rentalId:s.rentalId,lines:[{lineId:s.lineId,commercialTerms:{billingMethod:'Per Hour',unitRate:1000,minimumBillableHours:0,overtimeRate:0,standbyRate:0,mobilizationFee:0,demobilizationFee:0,fuelCharge:0,operatorIncluded:true,operatorRate:0,taxRate:0,withholdingTax:0,contractAmount:0,currency:'PHP'},costCodeId:rr.costCodeId,activityCodeId:rr.activityCodeId,workDescriptionId:rr.workDescriptionId,operationalRemarks:'Synthetic isolated-UAT offline restart runtime certification.',deurPolicy:{frequency:'PER_WORKDAY',effectiveFrom:DATE},shiftWindows:[],workDate:DATE,meterRequirement:'hourMeter'}]});await canonical(client,'RELEASE','command_release_rental',{...id('RELEASE','rental'),rentalId:s.rentalId,expectedVersion:2});await canonical(client,'ACTIVATE','command_activate_rental',{...id('ACTIVATE','rental'),rentalId:s.rentalId,expectedVersion:3});await a.service.schema('erp').rpc('complete_uat_deur_offline_restart_runtime_scenario',{command:{companyId:a.tenant,scenarioKey:KEY}});return inspect(a.service,a.tenant);}catch(error){const [failedStage,failedCode]=(error instanceof Error?error.message:'UNKNOWN').split(':',2);return out(409,{success:false,code:'UAT_OFFLINE_RESTART_SCENARIO_FAILED',failedStage,failedCode});}}
export async function inspectUatDeurOfflineRestartDomain(request:Request,env:GroupedReviewWorkerEnvironment){if(!fixed(await request.json().catch(()=>null)))return out(400,{success:false,code:'VALIDATION_REJECTED'});const a=await actor(request,env);return a?inspect(a.service,a.tenant):out(401,{success:false,code:'UNAUTHENTICATED'});}
